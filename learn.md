# G1 MuJoCo 强化学习笔记

## 系统设计

训练技术栈为 MuJoCo 3.4、Gymnasium、Stable-Baselines3 PPO 和 CPU 版
PyTorch。这台电脑没有支持 CUDA 的 GPU，因此采用 6 个 MuJoCo 子进程和规模适中的
多层感知机策略网络。

策略控制 15 个位置目标：

- 每条腿 6 个关节：髋关节俯仰、横滚、偏航，膝关节，踝关节俯仰、横滚
- 腰部 3 个关节：偏航、横滚、俯仰

其余 14 个手臂和手腕关节保持放松参考姿态。缩小动作空间可以降低初期站立与行走训练难度。

## 仿真频率

- MuJoCo 时间步：0.002 秒（500Hz）
- 每次策略动作执行：10 个物理步
- 策略时间步：0.02 秒（50Hz）
- 最大回合长度：20 秒（1000 个策略步）

## 观测空间

观测向量共 57 维：

| 观测内容 | 维度 |
| --- | ---: |
| 基座线速度 | 3 |
| 基座角速度 | 3 |
| 投影到骨盆坐标系的重力方向 | 3 |
| 受控关节位置误差 | 15 |
| 受控关节速度 | 15 |
| 上一次动作 | 15 |
| 速度指令 | 3 |

投影重力向量可以直接提供横滚和俯仰信息，并避免四元数表示的不连续问题。上一次动作有助于策略生成平滑控制。

## 动作空间

每个动作被限制在 `[-1, 1]`，按不同关节的动作尺度缩放后，叠加到轻微屈膝的默认姿态。
MuJoCo 位置执行器随后使用内部 PD 控制实现目标角度。训练初期采用位置目标比直接学习原始力矩更安全，也更容易优化。

## 奖励设计

正向奖励包括：

- 前向速度指令跟踪
- 抑制侧向速度
- 偏航角速度跟踪
- 骨盆保持直立
- 骨盆高度接近 0.76m

正则项用于惩罚动作突变和过大的关节速度。出现以下情况时回合终止：骨盆高度低于
0.50m、骨盆直立度低于 0.55，或仿真状态出现非有限数值。

## 如何判断训练效果

Rollout 奖励不能单独证明策略成功。机器人可能在摔倒前短暂获得较高奖励。主要验收条件为：

1. 独立确定性评估能够跑满 1000 个策略步。
2. 骨盆高度和直立度在 20 秒内保持稳定。
3. MuJoCo 界面中没有利用奖励漏洞的异常姿态或剧烈振荡。
4. 行走阶段的实际速度能够跟随目标速度，而不是无条件快速前冲。

第一次 99996 步评估仅存活约 1.79 秒。它是训练管线检查点，不是有效运动成果。

## 分阶段训练原因

从随机动作直接学习行走时，人形机器人的有效存活时间很短，学习信号不足。第一阶段将目标速度固定为
0m/s，让 PPO 优先学习平衡。第二阶段保留站立策略权重，并把速度指令恢复为 0～0.7m/s。

如果第一阶段经过较长训练仍无法接近完整回合，应优先收紧站立课程，而不是简单增加训练步数。
可尝试缩小动作尺度、降低初始动作标准差、逐步增大复位扰动，以及提高早期存活奖励权重。

## 第二版高效课程

实测表明站立策略在约 30 万步时已经能够通过 20 秒评估，原定 500 万步明显过量。
第二版课程直接加载约 50 万步的最佳站立模型，并采用以下改进：

- 速度上限按 0.10、0.25、0.45、0.70m/s 逐级提高。
- 学习率从 `3e-4` 降至 `1e-4`。
- 熵系数从 `0.005` 降至 `0.001`。
- 首段动作标准差从约 `1.0` 重置为 `0.3`。
- 策略残差动作尺度缩小一半。
- 使用正弦和余弦步态时钟协调左右腿。
- 添加低幅度参考步态、足端高度奖励和摔倒惩罚。
- 每段连续三次达到速度跟踪标准后提前结束。

为了兼容已有 57 维站立策略，步态时钟复用原来始终为零的两个指令观测槽。
首次迁移时将三个指令输入对应的网络权重清零，使初始行为仍然是已经验证过的稳定站立，
然后再逐步学习速度指令和步态相位。

## 文件说明

- `env.py`：Gymnasium 环境、观测、动作、奖励和终止条件
- `train.py`：PPO 配置、并行环境、独立评估和检查点
- `evaluate.py`：确定性无界面评估或 MuJoCo 界面评估
- `run_curriculum.sh`：从站立到行走的两阶段训练脚本
- `run_stand.sh`：只训练站立策略，为渐进行走课程生成起点模型
- `run_curriculum_v2.sh`：0.10、0.25、0.45、0.70m/s 四段渐进行走课程
- `progress_sync.py`：解析日志并更新实时训练状态
- `progress.md`：当前实测进度和常用操作命令
