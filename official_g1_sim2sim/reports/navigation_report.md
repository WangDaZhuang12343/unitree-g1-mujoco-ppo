# G1 视觉绕障闭环报告

生成时间：2026-08-05 14:30 CST

## 测试配置

- 目标点：世界坐标 `(5.0, 0.0)` 米
- 障碍物：中心 `x=2.5` 米，高度 `0.15` 米，宽度 `0.8` 米
- 感知：MuJoCo `mj_multiRay`，64×40，10 Hz
- 地图：机器人局部坐标瞬时代价地图，分辨率 5 厘米
- 规划：实测响应系数约束的 DWA，`vx=0.25～0.45`、`vy=±0.10`、`omega=±0.20`
- 控制：Unitree 官方 G1 29DoF ONNX 策略，50 Hz

## 验收结果

| 指标 | 结果 |
|---|---|
| 到达目标 | 是 |
| 到达时间 | 约 14.8 秒 |
| 最终目标距离 | 0.298 米 |
| 障碍物接触 | 无 |
| 跌倒 | 无 |
| 最大绕行横向位置 | 约 -0.88 米 |

原始轨迹：`final_candidate.csv`。

## 边界

当前通过的是单个独立障碍物绕行，不代表已经具备宽墙、多障碍、动态障碍或真机视觉能力。仿真通过几何 ID 精确剔除地面；真机必须替换为地面平面估计。
